[env]
# Use prebuilt LadybugDB shared library instead of building C++ from source.
# Run `make setup-lbug` to download the prebuilt library.
LBUG_SHARED = "1"
LBUG_LIBRARY_DIR = { value = "lbug-lib", relative = true }
LBUG_INCLUDE_DIR = { value = "lbug-lib", relative = true }
